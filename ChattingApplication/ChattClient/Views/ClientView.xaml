<UserControl x:Class="ChattClient.Views.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChattClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="350" Width="525">
    <Grid Margin="0,0,0,0">
        <Button x:Name="LoginBtn" Content="Login" Margin="192,10,0,0" VerticalAlignment="Top" Command="{Binding Login}" HorizontalAlignment="Left" Width="73"/>
        <TextBox x:Name="userNameTxtBx" Text="{Binding UserName}" HorizontalAlignment="Left" Height="20" Margin="10,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="177"/>
        <Button x:Name="SendBtn" Content="Send" Margin="0,0,170,10" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="75" Command="{Binding Send}" IsEnabled="True"/>
        <TextBox x:Name="MessageAreaTxtBox" Text="{Binding Message}" Margin="10,0,250,10" Height="20" VerticalAlignment="Bottom"></TextBox>
        <Label x:Name="WelcomeLbl" Content="{Binding UserName}" HorizontalAlignment="Right" Margin="0,10,10,0" VerticalAlignment="Top" Width="129" Height="25" FontSize="10"/>
        <TextBox x:Name="TextAreaTxtBox" Text="{Binding Chat}" Margin="10,40,142,47" TextWrapping="Wrap" IsReadOnly="True" IsUndoEnabled="False"/>
        <Button x:Name="LogoutBtn" Content="Logout" Visibility="Collapsed" HorizontalAlignment="Left" Margin="192,10,0,0" VerticalAlignment="Top" Width="74" IsEnabled="False" Command="{Binding Logout}"/>
        <ListBox x:Name="UsersListBox" ItemsSource="{Binding Users}" Margin="394,40,10,47"/>
    </Grid>
</UserControl>
